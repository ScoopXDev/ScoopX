<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="ScoopX.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScoopX"
    xmlns:controls="using:ScoopX.Controls">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <Color x:Key="SystemAccentColor">#2563EB</Color>
                    <Color x:Key="SystemAccentColorLight1">#3B82F6</Color>
                    <Color x:Key="SystemAccentColorLight2">#60A5FA</Color>
                    <Color x:Key="SystemAccentColorLight3">#93C5FD</Color>
                    <Color x:Key="SystemAccentColorDark1">#1D4ED8</Color>
                    <Color x:Key="SystemAccentColorDark2">#1E40AF</Color>
                    <Color x:Key="SystemAccentColorDark3">#1E3A8A</Color>
                    <SolidColorBrush x:Key="SystemControlHighlightAccentBrush" Color="#2563EB" />
                    <SolidColorBrush x:Key="AccentFillColorDefaultBrush" Color="#2563EB" />
                    <SolidColorBrush x:Key="AccentFillColorSecondaryBrush" Color="#CC2563EB" />
                    <SolidColorBrush x:Key="AccentFillColorTertiaryBrush" Color="#992563EB" />
                    <SolidColorBrush x:Key="AccentTextFillColorPrimaryBrush" Color="#2563EB" />
                    <SolidColorBrush x:Key="AccentTextFillColorSecondaryBrush" Color="#CC2563EB" />
                    <SolidColorBrush x:Key="AccentTextFillColorTertiaryBrush" Color="#992563EB" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <Color x:Key="SystemAccentColor">#3B82F6</Color>
                    <Color x:Key="SystemAccentColorLight1">#60A5FA</Color>
                    <Color x:Key="SystemAccentColorLight2">#93C5FD</Color>
                    <Color x:Key="SystemAccentColorLight3">#BFDBFE</Color>
                    <Color x:Key="SystemAccentColorDark1">#2563EB</Color>
                    <Color x:Key="SystemAccentColorDark2">#1D4ED8</Color>
                    <Color x:Key="SystemAccentColorDark3">#1E40AF</Color>
                    <SolidColorBrush x:Key="SystemControlHighlightAccentBrush" Color="#3B82F6" />
                    <SolidColorBrush x:Key="AccentFillColorDefaultBrush" Color="#3B82F6" />
                    <SolidColorBrush x:Key="AccentFillColorSecondaryBrush" Color="#CC3B82F6" />
                    <SolidColorBrush x:Key="AccentFillColorTertiaryBrush" Color="#993B82F6" />
                    <SolidColorBrush x:Key="AccentTextFillColorPrimaryBrush" Color="#3B82F6" />
                    <SolidColorBrush x:Key="AccentTextFillColorSecondaryBrush" Color="#CC3B82F6" />
                    <SolidColorBrush x:Key="AccentTextFillColorTertiaryBrush" Color="#993B82F6" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>

            <!-- 全局主色 = 侧栏选中色 #2563EB -->
            <Color x:Key="ScoopAccentColor">#2563EB</Color>
            <SolidColorBrush x:Key="ScoopAccentBrush" Color="{StaticResource ScoopAccentColor}" />

            <!-- Logo 蓝紫粉 → 浅色氛围渐变（Toolbox 式） -->
            <LinearGradientBrush x:Key="ScoopAmbientGradient" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#EEF2FF" Offset="0" />
                <GradientStop Color="#F5F3FF" Offset="0.38" />
                <GradientStop Color="#FDF2F8" Offset="0.72" />
                <GradientStop Color="#EFF6FF" Offset="1" />
            </LinearGradientBrush>

            <!-- Apple 式毛玻璃：应用内 Acrylic 模糊背后渐变 -->
            <AcrylicBrush
                x:Key="ScoopGlassAcrylicBrush"
                TintColor="#FFFFFF"
                TintOpacity="0.42"
                TintLuminosityOpacity="0.92"
                FallbackColor="#F4F6FB" />
            <AcrylicBrush
                x:Key="ScoopGlassStrongAcrylicBrush"
                TintColor="#FFFFFF"
                TintOpacity="0.62"
                TintLuminosityOpacity="0.95"
                FallbackColor="#FAFBFE" />
            <SolidColorBrush x:Key="ScoopGlassBorderBrush" Color="#B3FFFFFF" />

            <Style x:Key="ScoopGlassCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource ScoopGlassAcrylicBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource ScoopGlassBorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="16" />
            </Style>

            <!-- 表格内链接：无灰底选中/悬停块 -->
            <Style x:Key="TableLinkButtonStyle" TargetType="HyperlinkButton">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="MinHeight" Value="0" />
                <Setter Property="CornerRadius" Value="0" />
                <Setter Property="UseSystemFocusVisuals" Value="False" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="HyperlinkButton">
                            <ContentPresenter
                                x:Name="ContentPresenter"
                                Background="Transparent"
                                BorderThickness="0"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                ContentTransitions="{TemplateBinding ContentTransitions}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Padding="{TemplateBinding Padding}" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="DialogFieldLabelStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="#1F2937" />
            </Style>

            <Style x:Key="DialogFieldHintStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Foreground" Value="#9CA3AF" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <SolidColorBrush x:Key="SoftInputFillBrush" Color="#F2F3F5" />

            <Style x:Key="SoftCancelButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Background" Value="{StaticResource SoftInputFillBrush}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Foreground" Value="#374151" />
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="MinHeight" Value="36" />
                <Setter Property="Padding" Value="20,8" />
            </Style>

            <Style x:Key="SoftConfirmButtonStyle" TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="MinHeight" Value="36" />
                <Setter Property="Padding" Value="20,8" />
                <Setter Property="BorderThickness" Value="0" />
            </Style>

            <Style x:Key="SidebarNavButtonStyle" TargetType="controls:HandCursorButton">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="MinHeight" Value="0" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                <Setter Property="CornerRadius" Value="0" />
                <Setter Property="UseSystemFocusVisuals" Value="False" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="controls:HandCursorButton">
                            <ContentPresenter
                                Background="Transparent"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="CaptionButtonStyle" TargetType="controls:HandCursorButton">
                <Setter Property="Width" Value="46" />
                <Setter Property="Height" Value="32" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="MinHeight" Value="0" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="CornerRadius" Value="0" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="UseSystemFocusVisuals" Value="False" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="controls:HandCursorButton">
                            <Border
                                x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0">
                                <ContentPresenter
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}" />
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="#14000000" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="#26000000" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled" />
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="CaptionCloseButtonStyle" TargetType="controls:HandCursorButton" BasedOn="{StaticResource CaptionButtonStyle}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="controls:HandCursorButton">
                            <Border
                                x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0">
                                <ContentPresenter
                                    x:Name="Content"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}"
                                    Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="#E81123" />
                                                <Setter Target="Content.Foreground" Value="White" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="#F1707A" />
                                                <Setter Target="Content.Foreground" Value="White" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled" />
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
